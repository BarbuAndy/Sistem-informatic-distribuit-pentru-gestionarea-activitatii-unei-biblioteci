@model IEnumerable<WebApplication.Models.BranchModel>

@{
    ViewData["Title"] = "Inventory";
}
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<head>

    <meta charset="utf-8" />
    <title></title>
    <link rel="stylesheet" type="text/css" href="@Url.Content("~/CSS/Inventory.css")" />
    <link rel="stylesheet" type="text/css" href="@Url.Content("~/CSS/autocomplete.css")" />

</head>

<body>
    <div class="divBg">

        <table border="1">
            <thead>
                <tr>
                    <th>
                        Branch Name
                    </th>
                    <th>
                        Location
                    </th>
                    <th>
                        Borrow
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var branch in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => branch.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => branch.Location)
                        </td>
                        <td>
                            <input type="button" 
                                   onclick='window.location = "@Url.Action("BorrowSpecific", "Borrow")" +"?branchname=" + "@branch.Name" +"&"+ "booktitle=" + window.location '
                                    value = "borrow"
                                   />
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</body>
 

